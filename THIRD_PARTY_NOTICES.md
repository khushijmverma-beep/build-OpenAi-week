# Third-party notices

The current package has no third-party Swift package dependency. It uses Apple system frameworks (SwiftUI, AppKit, AVFoundation, Speech, Security, Carbon, Accessibility, SQLite) under the operating system/Xcode terms. The design evaluates WhisperKit as a future local-model adapter, but it is not compiled into the current dependency graph.
